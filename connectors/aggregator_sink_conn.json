{
  "name": "aggregator-sink-connector",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "2",
    "connection.url": "jdbc:postgresql://agregator-postgres:5432/aggregator",
    "connection.user": "postgres",
    "connection.password": "postgres",
    "topics": "data.ms.sennder",
    "pk.fields": "id",
    "pk.mode": "record_key",
    "auto.create": "true",
    "auto.evolve": "true",
    "insert.mode": "upsert",
    "delete.enabled": "true"
  }
}